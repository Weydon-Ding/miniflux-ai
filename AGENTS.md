## Agent skills

### Issue tracker

Issues 记录在 GitHub Issues 中，并使用 `gh` CLI 操作。详见 `docs/agents/issue-tracker.md`。

### Triage labels

Triage 使用默认五个 canonical labels：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

这是 single-context repo：读取根目录 `CONTEXT.md`，以及相关的 `docs/adr/` ADRs（如果存在）。详见 `docs/agents/domain.md`。
